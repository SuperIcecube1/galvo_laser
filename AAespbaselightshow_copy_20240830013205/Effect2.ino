#include <FastLED.h>

void Effect2() {
    // Code for Effect1, like LED animations
    int glitch_timer = 0;  // Example variable

    // Example of a random glitch effect
    if (glitch_timer == 0 && random(0, 100) < 5) {
        // Some code here
    }
    
    // Other code for Effect1

}  // Ensure this closing brace exists
